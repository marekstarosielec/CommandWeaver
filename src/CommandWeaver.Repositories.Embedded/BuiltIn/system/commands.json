{
  "commands": [
    {
      "name": "cmd",
      "description": "source json of single single command",
      "category": "test category",
      "parameters": [
        {
          "key": "name",
          "otherNames": ["n"],
          "description": "Name of command",
          "required": true
        }
      ],
      "operations": [
        {
          "operation": "terminate",
          "message": "Command {{name}} does not exist",
          "conditions": {
            "isNull": "{{ commands[{{name}}] }}"
          }
        },
        {
          "operation": "output",
          "value": "{{ commands[{{name}}].source }}",
          "styling": "json"
        }
      ]
    }
  ]
}