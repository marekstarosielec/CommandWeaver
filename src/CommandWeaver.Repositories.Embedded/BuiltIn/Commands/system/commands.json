{
  "commands": [
    {
      "name": "cmd",
      "parameters": [
        {
          "key": "name",
          "otherNames": ["n"],
          "description": "Name of command",
          "required": true
        }
      ],
      "operations": [
        {
          "operation": "terminate",
          "message": "Command {{name}} does not exist",
          "conditions": {
            "isNull": "{{ commands[{{name}}] }}"
          }
        },
        {
          "operation": "output",
          "text": "Command [[#0000FF]]{{ name }}[[/]] in [[u]]{{commands[{{name}}].id}}[[/]]"
        },
        {
          "operation": "output",
          "text": "{{ commands[{{name}}].json }}",
          "formatting": "raw"
        }
      ]
    }
  ]
}