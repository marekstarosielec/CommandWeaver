{
  "commands": [
    {
      "name": "paths",
      "operations": [
        {
          "operation": "output",
          "text": "Local path: {{ LocalPath }}"
        },
        {
          "operation": "output",
          "text": "Current session path: {{ SessionPath }}"
        }
      ]
    }
  ]
}